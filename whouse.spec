# -*- mode: python -*-
a = Analysis(['whouse.py'],
             pathex=['C:\\Users\\Philip\\My Documents\\github\\gitWH\\gitWH'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='whouse.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='whouse')
